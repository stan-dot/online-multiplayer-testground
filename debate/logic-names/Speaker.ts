//also always has included as audience in any text
export default class Speaker {
  constructor(name) {
    (this.name = name), (this.positions = []), (this.topics = []);
  }
  //also need to create default position if they don't state it
  //they also can define themselves

  //emotional state

  //they can add topics so that later can see who criticizes what
  //we get the Jain epistemology as an effect
  addTopic() {}

  getCriticizedTopics() {}

  changePositions(position) {}
}
