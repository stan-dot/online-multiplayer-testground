export default class Div {
  //it's aprt of the topic and the linear view
  constructor(name, statements) {
    (this.name = name), (this.statemts = statements);
  }

  //also set the allowed names as some enum
}
