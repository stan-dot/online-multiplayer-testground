export default class LogicThread {
  constructor(telos) {
    this.telos = telos; //the telos is the research question,
    //the thing that we are investigating - it has a locally imporatnt place
  }
  // TODO: might join with the logicModel
  //extract not the whole structure, but just the bits relevant to the quesiton
  //tbf, might do that for any specified telos; that is the orle of the logic modules
  extractProof() {}
  extrapolate() {
    //seek in the global space noncontradictory logical maps
  }
}
