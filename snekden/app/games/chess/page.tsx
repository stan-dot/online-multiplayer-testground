"use client";

import { useEffect, useRef, useState } from "react";

type BoardDimensions = { x: number, y: number };

const DEFAULT_DIMENSIONS: BoardDimensions = { x: 8, y: 8 }

export default function ChessGame() {


  return (
    <div>
      <p>here some chess implementation</p>

    </div>
  );
}
